/**
 * Shared application state.
 * @type {{lastAnswer: string, darkMode: boolean, degreeMode: boolean, memoryValue: null}}
 */
const appState = {
    lastAnswer: "",
    darkMode: false,
    degreeMode: true,
    memoryValue: null
};